#Make sure current working directory is set to where the files are, else the tables and files will not
be detected#

#The column names for each csv file were deleted, since I didn't know how to ignore them and 
they were causing datatype mismatch errors when loaded into the database#

Task.py should be able to be run by just hitting the run button.
It will create the database and then load the data into it. 

summary.py connects to Project.db and all operations will be printed. An example of Project.db
is provided

update.py was not completed due to lack of time 